import { analyzeNotesWithGemini } from './src/utils/aiAnalyzer.ts';

// Wait, the project is using Vite and TypeScript, running ts files directly might need ts-node or tsx.
// Let's just create a raw fetch script to be safe.
